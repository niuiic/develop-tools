[project-run]

command=sh .task.sh projectRun

cwd=$(VIM_ROOT)

output=terminal

[clean-mem]

command=sh .task.sh cleanMem

cwd=$(VIM_ROOT)

output=terminal

[reopen-firefox]

command=sh .task.sh reopenFirefox

cwd=$(VIM_ROOT)

output=terminal
