[project-build]

command=sh .tasks.sh build

cwd=$(VIM_ROOT)

output=quickfix

[project-run]

command=sh .tasks.sh run

cwd=$(VIM_ROOT)

output=terminal

[project-check]

command=sh .tasks.sh check

cwd=$(VIM_ROOT)

output=quickfix

[project-clean]

command=sh .tasks.sh clean

cwd=$(VIM_ROOT)

output=terminal

[project-init]

command=sh .tasks.sh init

cwd=$(VIM_ROOT)

output=terminal

[watch-wave]

command=sh .tasks.sh watch

cwd=$(VIM_ROOT)

output=terminal
