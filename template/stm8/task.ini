[project-run]
cwd=$(VIM_ROOT)
command=sh .tasks.sh run 
output=terminal

[project-build]
cwd=$(VIM_ROOT)
command=sh .tasks.sh build
output=quickfix

[project-clean]
cwd=$(VIM_ROOT)
command=sh .tasks.sh clean
output=terminal

[get-header]
cwd=$(VIM_ROOT)
command=sh .tasks.sh get
output=terminal
