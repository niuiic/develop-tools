[project-run]

command=yarn dev

cwd=$(VIM_ROOT)

output=terminal

[project-build]

command=yarn build

cwd=$(VIM_ROOT)

output=terminal

[project-preview]

command=yarn preview

cwd=$(VIM_ROOT)

output=terminal
