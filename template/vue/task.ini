[project-run]

command=sh .task.sh dev

cwd=$(VIM_ROOT)

output=terminal

pos=hide

[project-build]

command=sh .task.sh build

cwd=$(VIM_ROOT)

output=terminal

[project-preview]

command=sh .task.sh preview

cwd=$(VIM_ROOT)

output=terminal

[new-component]

command=sh .task.sh newComponent $(?pathToSrc) $(?name)

cwd=$(VIM_ROOT)

output=terminal
