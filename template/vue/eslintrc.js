export default {
  extends: ["alloy", "alloy/vue"],
  env: {
    browser: true,
    node: true,
    // mocha: true,
    // jest: true,
    // jquery: true
  },
  globals: {
    // global variable here
    // set false to prevent it from being reassigned
  },
  rules: {
    // custom rules
  },
};
