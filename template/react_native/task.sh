if [[ $1 == "projectRun" ]]; then
    echo "run 'pro react-native start' first, and run 'pro yarn android' in another ternimal"
    echo "This will cause an error, but don't worry. Just run 'pro yarn android' again and click 'reload' button on the emulator. Then everything goes perfect."
fi
