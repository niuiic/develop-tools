[project-build]

command=sh .tasks.sh build

cwd=$(VIM_ROOT)

output=quickfix

errorformat=%f:%l:%m

[project-run]

command=sh .tasks.sh run

cwd=$(VIM_ROOT)

output=terminal

[project-test]

command=sh .tasks.sh test

cwd=$(VIM_ROOT)

output=quickfix

errorformat=%f:%l:%m

[example-build]

command=sh .tasks.sh build-example $(?target to build)

cwd=$(VIM_ROOT)

output=quickfix

errorformat=%f:%l:%m

[example-run]

command=sh .tasks.sh run-example $(?target to run)

cwd=$(VIM_ROOT)

output=terminal

[project-concrete-build]

command=sh .tasks.sh concrete-build $(?target to build)

cwd=$(VIM_ROOT)

output=quickfix

errorformat=%f:%l:%m

[project-concrete-run]

command=sh .tasks.sh concrete-run $(?target to run)

cwd=$(VIM_ROOT)

output=terminal

[project-concrete-test]

command=sh .tasks.sh concrete-test $(?target to tested)

cwd=$(VIM_ROOT)

output=quickfix

errorformat=%f:%l:%m

[project-clean]

command=sh .tasks.sh clean

cwd=$(VIM_ROOT)

output=terminal
