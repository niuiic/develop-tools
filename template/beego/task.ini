[project-run]

command=echo "run \`bee run -gendoc=true -downdoc=true\` on inner terminal."

cwd=$(VIM_ROOT)

output=terminal

[project-debug]

command=firefox-bin --new-tab http://127.0.0.1:8080/swagger

cwd=$(VIM_ROOT)

output=terminal
